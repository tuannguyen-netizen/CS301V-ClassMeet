# MongoDB Configuration
# Local: Dùng MongoDB cục bộ (mongodb://localhost:27017/classmeet)
# Public: Dùng MongoDB Atlas với thông tin bảo mật
MONGODB_URI=mongodb+srv://admin:securepassword@cluster0.4d6piad.mongodb.net/classmeet?retryWrites=true&w=majority&appName=Cluster0

# Security Configuration
# Tạo khóa mạnh: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secure-random-secret-key-here

# Server Configuration
# Local: HOST=127.0.0.1 (chỉ localhost) hoặc 0.0.0.0 (mạng nội bộ)
# Public: HOST=0.0.0.0 (bắt buộc để truy cập từ ngoài)
HOST=0.0.0.0
PORT=5000
DEBUG=True

# JWT Configuration
JWT_EXPIRATION=60